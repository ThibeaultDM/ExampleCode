<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:local="clr-namespace:ModuleCustomer" xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" x:Class="ModuleCustomer.CustomerView"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <Grid x:Name="LayoutRoot" Background="White" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListBox x:Name="_listCustomers" ItemsSource="{Binding ListCustomers}">
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="SelectionChanged">
                    <prism:InvokeCommandAction Command="{Binding CustomerSelectedCommand}" CommandParameter="{Binding SelectedItem, ElementName=_listCustomers}" />
            </i:EventTrigger>
        </i:Interaction.Triggers>
        </ListBox>
        <ContentControl Grid.Row="1" Margin="10" prism:RegionManager.RegionName="CustomerDetailsRegion" />
    </Grid>
</UserControl>